# 验证记录

验证日期：2026-09-10。环境：Windows、Python 3.9.12、PyTorch 2.4.0+cu118，所有计算使用 CPU。

## 已通过

```bash
python -m unittest discover -s . -p "test_*.py" -v
```

现有 8 项测试全部通过，测试耗时 2.656 秒（不含 Python 启动）。

| 范围 | 验证内容 |
| --- | --- |
| 归一化与前馈层 | RMSNorm 数值、SwiGLU 梯度有限、RoPE 保范数与 buffer 持久化行为 |
| CachedGQA | 完整序列与分块缓存一致、因果性、padding / 全屏蔽、有限反向梯度 |
| LoRA | 初始输出一致、A/B 梯度、基础层冻结、合并导出等价且不重复叠加 |
| Mask 损失 | Causal LM 标签位移与有效位置、空有效集、masked MSE 梯度 |
| 对比损失 | 单正样本与交叉熵等价、多正样本置换一致性 |
| Flow | 训练梯度有限、已知恒定速度下 Euler 积分和 CFG 输出 |
| Top-K LoRA | 校准排名、状态恢复、未使用层排除、注入后的可训练参数、空数据异常恢复 |

此外，13 个组件文件的 `__main__` 示例均在 CPU 上运行成功；逐个执行前设置随机种子 42。所有 Python 文件通过语法解析，常见密钥模式扫描未命中。

## 验证边界

- 示例运行只验证接口、形状和执行路径；不能替代性质测试。
- 未验证 CUDA、混合精度、多卡、编译、量化和跨版本兼容性。
- 未测试真实任务效果、训练收敛、吞吐或显存表现。
- `torch>=2.0` 是声明的依赖范围，本次没有覆盖整个范围。
- Top-K 指标是局部梯度敏感度启发式，不是 Fisher 信息矩阵或收益保证。
- 未指定整体开源许可证。
